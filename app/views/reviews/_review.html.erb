<div class="review-rating" data-score="<%= review.rating %>"></div>
<p><%= review.comment %></p>

<% if usuario_signed_in? %>

    <%if review.usuario_id == current_usuario.id %>

    <%= link_to "Editar", edit_room_review_path(review.room, review) %>
    <%= link_to "Eliminar", room_review_path(review.room, review), method: :delete, data: {
        confirm: "¿Estás seguro de que quieres eliminar este comentario?"} %>

        <% end %>

<% end %>